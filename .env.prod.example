# Шаблон секретов/настроек прод-стека Epharm.
# Скопировать в .env.prod (НЕ коммитится) и заполнить реальными значениями:
#   cp .env.prod.example .env.prod && nano .env.prod
# Запуск:
#   docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build

# ── Домены + TLS (Caddy + Let's Encrypt) ─────────────────────────────────────
# epharm.inkar.kz — единый публичный домен: /api/* и /s3/* маршрутизируются Caddy.
API_DOMAIN=epharm.inkar.kz      # кассы (/api/posm) и телефоны (/api/mobile)
ADMIN_DOMAIN=epharm.inkar.kz    # веб-админка
S3_DOMAIN=epharm.inkar.kz       # публичные фото чеков/слайды через /s3/*
ACME_EMAIL=admin@inkar.kz       # email для Let's Encrypt (уведомления о сертификатах)
# Закрытая директория с fullchain.pem и private.key для wildcard *.inkar.kz.
# Не коммитить. На сервере: root:root, каталог 700, private.key 600.
TLS_CERT_DIR=./tls

# ── База данных ──────────────────────────────────────────────────────────────
POSTGRES_DB=epharm
POSTGRES_USER=epharm
POSTGRES_PASSWORD=                 # ОБЯЗАТЕЛЬНО: длинный случайный пароль

# ── MinIO / S3 (фото чеков, слайды) ──────────────────────────────────────────
MINIO_ROOT_USER=epharm
MINIO_ROOT_PASSWORD=               # ОБЯЗАТЕЛЬНО: длинный случайный пароль
MINIO_CONSOLE_PORT=9001            # консоль администрирования (закрыть файрволом/VPN)
# Внутренний адрес для backend SDK (PUT/DELETE) — внутри docker-сети, не менять.
S3_ENDPOINT=http://minio:9000
# Внешний адрес для ссылок в браузере — через Caddy по https и /s3.
S3_PUBLIC_URL=https://epharm.inkar.kz/s3
S3_REGION=us-east-1

# ── Backend секреты ──────────────────────────────────────────────────────────
# JWT_SECRET: МИНИМУМ 256 бит (32+ симв). Сгенерировать: openssl rand -base64 48
JWT_SECRET=
# Переходный fleet-key только для короткого окна legacy enrollment.
# Оставить пустым при POSM_LEGACY_DEVICE_KEY_ENABLED=false; если окно нужно:
# openssl rand -hex 24
POSM_DEVICE_KEY=
# false после предварительной выдачи индивидуального токена каждой кассе.
POSM_LEGACY_DEVICE_KEY_ENABLED=false
# Base64 DER SubjectPublicKeyInfo публичного ECDSA P-256 ключа подписи update manifest.
POSM_UPDATE_PUBLIC_KEY_SPKI=
POSM_FISCAL_ARTIFACT_TRUSTED_SOURCES=standardn-kkm-sdk,ofd-api
# Optional merchandising task bridge. Keep disabled until the server-to-server
# key is installed and both the task API and public QR portal pass smoke tests.
# Public Internet HTTP origins are rejected when a server key is present. Use
# HTTPS for an external fallback; plain HTTP is only for trusted private/LAN IPs.
MERCH_TASKS_ENABLED=false
MERCH_TASKS_BASE_URL=http://10.10.1.80:8080
MERCH_TASKS_INTEGRATION_KEY=
MERCH_TASKS_TIMEOUT_MS=7000
# Caddy accepts an HTTPS origin or a trusted private host:port (without a path).
# Only the staff portal, its assets and task-scoped media/API should be public.
MERCH_PORTAL_UPSTREAM=10.10.1.80:8080
# Сквозная выдача интернет-заказов. Сначала развернуть false, заполнить pharmacy links,
# затем включить только после smoke-test. Секрет: openssl rand -base64 48
FULFILLMENT_ENABLED=false
FULFILLMENT_SHARED_SECRET=
FULFILLMENT_SIGNATURE_SKEW_SECONDS=300
FULFILLMENT_DEVICE_REGISTRATION_ENABLED=false
# Leave empty until a server-to-server payment webhook producer has been reviewed.
FULFILLMENT_TRUSTED_CARD_PAYMENT_AUTHORITIES=
# Origins админки через запятую, без trailing slash.
CORS_ALLOWED_ORIGINS=https://epharm.inkar.kz
# OTP: false = реальный внешний SMS-код (прод). true = фиксированный 5445 (только dev!).
OTP_DEV_MODE=false
# Полный внешний OTP-цикл Daribar из swag.json: /api/v2/sms + /api/v2/auth.
OTP_PROVIDER=daribar
DARIBAR_OTP_BASE_URL=https://prod-backoffice.daribar.com
DARIBAR_OTP_TIMEOUT_MS=10000
# SMS-провайдер p1sms.kz (admin.p1sms.kz). API-ключ — в ЛК: «Эмулятор запросов»/«Инструкция по API».
# Используется только при OTP_PROVIDER=p1sms. Канал: digit | char (нужен P1SMS_SENDER).
P1SMS_API_KEY=
P1SMS_CHANNEL=digit
P1SMS_SENDER=
# Bootstrap первого admin-аккаунта HQ-консоли. БЕЗ них в админку не войти (admin_users пуста).
# Создаётся один раз при первом старте, если таблица пуста (роль HQ_HEAD). Пароль — надёжный.
ADMIN_BOOTSTRAP_EMAIL=
ADMIN_BOOTSTRAP_PASSWORD=
ADMIN_BOOTSTRAP_NAME=HQ Admin
ADMIN_BOOTSTRAP_COMPANY=Inkar

# ── Medusa-витрина inkar.kz (Module 3) — реальный каталог для мобилки ─────────
# Бэкенд ходит сюда server-to-server и проксирует товары в /api/mobile/catalog/*.
# publishable-ключ/канал/регион — клиентские идентификаторы (по дизайну Medusa они
# публикуемые, как в любом storefront), держать здесь безопасно. РЕАЛЬНЫЕ секреты
# витрины (SSH root, admin-пароли из STOREFRONT-CREDENTIALS.md) сюда НЕ кладём и в
# репозиторий не коммитим — их нужно ротировать (см. claude-admin-notes.md).
# Legacy-origin пока без TLS, поэтому backend разрешает только этот точный IP:port.
# Остальные удалённые HTTP URL по-прежнему запрещены. После появления HTTPS меняется
# только MEDUSA_BASE_URL. Эти четыре значения проверяются tools/smoke-medusa.sh.
MEDUSA_ENABLED=true
MEDUSA_BASE_URL=http://78.140.246.238:9000
MEDUSA_PUBLISHABLE_KEY=pk_ed9c35a59066b45de7d9e12510468ca27af16b6d0170b0910c03746545da4525
MEDUSA_SALES_CHANNEL_ID=sc_01KRXGQFYXMJN3FD1WJ7S83WME
MEDUSA_REGION_ID=reg_01KSBNEH2D4GVJN8EATK79WNSH
MEDUSA_CONNECT_TIMEOUT_MS=2000
MEDUSA_READ_TIMEOUT_MS=15000
MEDUSA_CACHE_TTL=300
MEDUSA_CACHE_STALE_IF_ERROR_SECONDS=3600
MEDUSA_SNAPSHOT_PAGE_SIZE=500
MEDUSA_SNAPSHOT_FETCH_WORKERS=1
MEDUSA_SNAPSHOT_READ_TIMEOUT_MS=60000
MEDUSA_SNAPSHOT_REFRESH_SECONDS=3600
MEDUSA_SNAPSHOT_RETRY_SECONDS=300
MEDUSA_SNAPSHOT_INITIAL_DELAY_MS=5000
MEDUSA_SNAPSHOT_POLL_MS=60000
# Связанные акции/POSM-товары обновляются из Medusa каждый час в :05 (Asia/Almaty).
PROMO_PRICE_REFRESH_CRON="0 5 * * * *"
# Публичный HTTPS-origin ePharm (без завершающего slash), используется в QR сертификатов.
APP_PUBLIC_BASE_URL=https://epharm.inkar.kz

# ── Release identity (normally generated in .release.env by tools/release/prepare.sh) ─
RELEASE_ID=dev
RELEASE_COMMIT=unknown

# ── Backups / monitoring ────────────────────────────────────────────────────
BACKUP_ROOT=/var/backups/epharm
BACKUP_RETENTION_DAYS=30
MINIO_BACKUP_BUCKETS=epharm-receipts
PROMETHEUS_RETENTION=30d
PROMETHEUS_PORT=9090
ALERTMANAGER_PORT=9093
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=
# HTTPS webhook understood by the selected incident channel (Slack relay, Teams relay,
# PagerDuty Events proxy, etc.). Empty means alerts are visible but notifications are muted.
ALERT_WEBHOOK_URL=

# ── Sentry (empty DSN disables the SDK cleanly) ───────────────────────────────
SENTRY_BACKEND_DSN=
SENTRY_FRONTEND_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.05
# Source-map upload is build-time and optional. Keep the token in CI/secret storage.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_FRONTEND_PROJECT=
