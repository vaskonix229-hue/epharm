# Аудит заказов, QR-мерчендайзинга и POSM — 2026-09-23

Статус документа: первая часть фиксирует проверку исходного `origin/main` (`c24ed9b`) и живых контуров 2026-09-23 около 01:15–02:00 UTC; результаты после исправлений и выкладки приведены в конце. Число онлайн-касс и состояние очередей — моментальный срез, а не гарантия на весь день. В ходе аудита тестовые заказ, QR-задание и фотоотчёт не создавались и не изменялись; позже был обнаружен один настоящий новый заказ после cutoff, прошедший штатный транспорт. Значения ключей, токенов и учётных данных в отчёт не включены.

## Контракт, по которому оценивали

- Документ INKAR «Интеграция мерчендайзинга с ePharm» от 21.09.2026: касса обращается только к ePharm; телефон открывает HTTPS `/merch/staff` без VPN; показ и выполнение задания подтверждаются отдельно; production-секрет нельзя передавать по публичному HTTP.
- Документ «Интеграция АСС и PharmaPay» от 21.09.2026: атомарный outbox, HMAC на точных байтах запроса, точное сопоставление аптек, индивидуальные device tokens, однонаправленные версии статуса, запрет карточной выдачи без подтверждённого источника оплаты.
- Оба документа фиксируют исходный пилотный статус на 21.09 и не являются доказательством текущего production: worker АСС теперь включён, а rollout allowlist расширен.

## Результаты

| Область | Результат на момент аудита | Доказательство / ограничение |
| --- | --- | --- |
| ePharm, ACC и merch health | PASS | Публичные health ePharm/ACC и merch ответили 200; каталог ePharm имеет готовый snapshot из 28 503 товаров. |
| ACC → ePharm транспорт заказов | PARTIAL | Timer активен, подписанный status feed ответил 200; после cutoff от 22.09 21:26 UTC не появилось ни одного нового заказа/outbox event. Успешный реальный цикл после включения ещё не доказан. |
| Идентичность аптек и касс | PASS для текущих онлайн | 477 аптек в явном allowlist АСС; 89 активных индивидуальных fulfillment devices в 81 аптеке. Все 81 текущие онлайн-аптеки входят в allowlist. Чужая очередь без token и unsigned feed дают 401. Офлайн-аптеки индивидуально не проверены. |
| Выдача карточного заказа | BLOCKED | Worker не посылает `paymentAuthority`, доверенный список backend пуст, заявленный `paid` понижается до `pending`; последующего signed payment-update API нет. Для controlled acceptance разрешены только cash или явно demo сценарии. |
| QR-страница задания | FAIL | HTML `/merch/staff` доступен, но его root-relative JS/CSS попадали в ePharm frontend и возвращали 404. После загрузки бандла root-relative task API, фото и проверка `/staff` всё равно были бы неверны. Это воспроизводимая причина жалобы на «белую/непрогружающуюся» страницу. |
| Публичная граница merch | FAIL | Старый proxy публикует почти весь CRM/API под `/merch/*`, а server-to-server key отправлялся на публичный `http://90.156.222.185:8080`. Это не соответствует интеграционному контракту. HTTPS `:8443` с проверенным сертификатом доступен с обеих сторон, но переключение и ротация ключа ещё не проведены. |
| Публичные demo admin-учётки merch | FAIL / INCIDENT | Три активные административные записи в production имеют пароли, встроенные в публичный SPA-бандл; совпадение проверено read-only через BCrypt hash без login/POST и без вывода значений. До публикации нового бандла нужен отзыв/ротация этих записей и удаление подсказок из исходника. |
| Кассовые рекомендации | PASS на уровне кода/непрерывности | Профильные backend tests и POSM Core tests зелёные; TaskKiosk и fulfillment имеют отдельные фоновые циклы с обработкой ошибок. Физическое одновременное прохождение рекомендации, заказа и QR на кассе в этот аудит не выполнялось. |
| POSM fleet update | PASS для онлайн-среза, PARTIAL для офлайн | Обязательный 1.0.63 подписан; CDN Range и полный ZIP/SHA-256 совпадают с release metadata. Все 88 онлайн-устройств в срезе сообщили `1.0.63.0`; обновление выключенных касс требует нового heartbeat после включения. |
| Release/rollback | PARTIAL | Живые backend/frontend соответствуют manifest `v0.1.9`, образы для `v0.1.8` есть. Caddy с bind-mounted конфигом требует отдельного validate/recreate и rollback его предыдущего файла. Off-site backup не обязателен в текущей конфигурации. |

## Review-находки и изменения в ветке

1. **P0 — QR + transport + demo accounts.** Подготовлен воспроизводимый patch отдельного merch SPA с относительными asset URL, mount-aware API/media/routing, восстановленным из работающего бандла отсутствующим `catalogCities.js` и тестами. ePharm Caddy ограничен точным allowlist staff-путей. Backend не отправляет ключ на публичный HTTP origin. Публичные demo admin credentials необходимо убрать из нового bundle без помещения их в Git; три соответствующие активные учётные записи требуют отзыва/ротации владельцем. Патч и исходные SHA — в `ops/merch-portal-patches/`.
2. **P1 — источник мерча не был в Git.** Патч хранится в ePharm, но полное приложение на 90.156.222.185 пока остаётся без собственного репозитория; любые будущие сборки требуют сверки базовых SHA и отдельного source-control решения.
3. **P1 — реальные деньги.** `paymentAuthority` нельзя добавлять по одному только значению браузера. Нужны согласованные server-verified событие оплаты, политика отмены/возврата и контракт обновления статуса после создания заказа. До этого карточная выдача остаётся fail-closed.
4. **P2 — изоляция offline-очереди.** Локальный кэш POSM теперь привязан к `(pharmacyId, deviceId)`, отвергает старый/чужой формат и восстанавливается после online poll; добавлены тесты смены аптеки и restart.
5. **P2 — установка/релиз.** Установщик POSM отвергает удалённый HTTP и некорректный явный SPKI, но сохраняет встроенный public key для legacy конфигов. Добавлены PowerShell preflight и release prepare→deploy contract tests.
6. **Открытые edge cases.** POSM при более чем 20 новых заказах за один poll показывает все в очереди, но уведомляет только о первых 20; повторяющаяся ошибка копирования обязательного POSM update может вызвать цикл рестартов. Нужны отдельные нагрузочный и Windows acceptance тесты.

## Проверки ветки до production

- Полный backend Gradle test suite — PASS; отдельно MerchTaskClient/Fulfillment/Recommendation integration tests — PASS.
- ACC bridge `node --test` — 11/11 PASS; signed feed на живой связке — HTTP 200.
- POSM Core — 97/97 PASS; Windows-targeted build — PASS; installer preflight — 9/9 локально.
- Админка: `npm ci`/audit — 0 уязвимостей, lint/test/build — PASS.
- Merch SPA в изолированной копии: `npm ci`, `npm audit --audit-level=low` (0 уязвимостей), `npm run build` и 6/6 mount/build/city tests — PASS после восстановления отсутствующего исходника. Sanitizer подтвердил отсутствие трёх известных пар demo credentials во всех 25 новых JS assets. Браузерный smoke через префикс `/merch/staff` загрузил React, JS/CSS и статический эталон без ошибок консоли; без действительного token портал показал ожидаемое сообщение об отсутствии доступа. Реальный production QR с valid token не проверялся, поскольку POST открытия меняет данные задания.
- Caddy adaptation test подтверждает HTTPS upstream в тестовой конфигурации, exact staff allowlist, default-deny и правильный порядок до frontend catch-all. Release prepare/deploy contract test — PASS.

## Go/No-Go для выкладки и демонстрации

Дополнительный preflight 2026-09-23 обнаружил 1 174 фото/каталожных файла (215 456 812 байт) в live `dist` сервера мерча, которых нет в Vite source и которые обычная сборка удалила бы. Патч дополнен проверяемым сохранением этих sidecar-каталогов, старых hash-named assets для открытых вкладок и атомарным обменом каталогов с обратным ходом. Прежний офлайн-ключ подписи POSM найден локально: его DER public SPKI совпал с pin backend и установленного клиента. На ePharm root LV онлайн увеличен на 30 GiB без удаления данных; после операции свободно 34 GiB.

Владелец 2026-09-23 явно решил оставить три demo admin-учётки активными для тестирования и принял риск их известности. Новый bundle удаляет автоподстановку, но старые JS assets временно сохраняются для открытых вкладок, поэтому считать пароль отозванным нельзя. Это **принятый остаточный риск**, а не устранённая уязвимость.

Перед выкладкой: GitHub PR с полными checks; проверенный backup исходников, `dist`, Caddy, env, БД и предыдущих образов; проверка всех media sidecars до/после обмена; test/recreate Caddy с сохранённым rollback-файлом; смена merch upstream на проверенный HTTPS `:8443`; ротация ранее переданного по HTTP server key; снятие прямого публичного доступа к HTTP 8080 без нарушения согласованного доступа сотрудников. Владелец отложил выбор аптеки и создание тестовых заданий/заказов до отдельного сообщения. Поэтому после выкладки допустимы только немутирующие health/asset/bridge/telemetry smoke-тесты; внешний телефонный тест с контролируемым token и новый cash/demo заказ на двух действующих кассах остаются **непройденной функциональной приёмкой**, а не подразумеваемым успехом.

При отказе: сначала откатить Caddy и его upstream, затем backend/frontend через предыдущие образы; merch `dist` и source — из проверенного backup. Не откатывать Flyway и не удалять outbox/заказы/задания. Любое повторение ошибки QR или bridge требует остановки дальнейшего rollout и read-only диагностики. Этот аудит сам по себе не является разрешением публиковать новую сборку или отзывать действующие учётные записи без согласования с владельцем.

## После исправлений и production-выкладки

На 2026-09-23 код PR [#67](https://github.com/sabirovv17/epharm/pull/67) вошёл в `main` коммитом `4d842893ae01879fc82603d8f8048e6c4e507555`; полный PR CI и повторный CI точного main-коммита (`35813914334`) завершились успешно. Тег `v0.1.10` развёрнут на ePharm из проверенного Git bundle с backup PostgreSQL, MinIO, внешней копией и smoke-проверками. Публичный `/api/health` отдаёт `v0.1.10`, каталог готов (28 503 товара), поиск проверен. ePharm root LV увеличен на 30 GiB перед релизом без удаления данных.

| Поток | Результат после выкладки | Что ещё не доказано |
| --- | --- | --- |
| QR-страница сотрудника | Новый portal bundle использует относительные пути, а ePharm Caddy публикует только разрешённые staff-маршруты. `/merch/staff`, JS/CSS и браузерная страница без token загружаются без ошибок; без token показан ожидаемый отказ. Сохранены и сверены все 1 174 sidecar-файла (215 456 812 байт), старые hash assets для открытых вкладок и root PNG. | Вход с настоящим QR-token, выполнение и фотоотчёт на выбранной аптеке отложены владельцем. Отсутствие token не является полноценным пользовательским acceptance. |
| Граница merch и server key | Прямой публичный HTTP `:8080` закрыт; backend обращается к проверенному HTTPS `:8443`, Nginx на 90 ведёт API только через loopback. Ключ моста ротирован; его значения живут только в защищённых env/локальном игнорируемом хранилище. | Три известные demo admin-учётки сохранены по явному решению владельца; старые assets временно оставлены для открытых вкладок. Риск доступа к демо-данным принят, не устранён. |
| Нагрузка QR-опроса | При сотнях касс фиксированный 10-секундный опрос привёл к всплескам 499 на 90 и `active/failure` на ePharm. Пул PostgreSQL merch увеличен с 2 до 16 при 24 API workers; после изменения failure-counter оставался на 1 064, а успешные запросы росли. PR [#68](https://github.com/sabirovv17/epharm/pull/68) заменил фиксированный опрос на распределённый интервал 24–36 секунд; PR CI (все 22 проверки) и повторный CI на `main` (`35819146595`) завершились успешно. | Требуется наблюдение после массового перехода POSM 1.0.65: двухъядерный merch-сервер и его диск остаются узким местом. При новых 499/timeout исследовать индекс/кэш и независимое масштабирование API, а не бесконечно повышать пул. |
| Заказ АСС → ePharm | Timer/worker активен, синхронизация включена на явном allowlist 477 аптек (SHA-256 `cf097e7aa8dcff1a4b3edbfbe2d4c1641ba0cbc52c627bab7c813afe166d8ed3`), старые события до cutoff не отсылаются. Один реальный новый заказ после cutoff прошёл outbox `sent` за одну попытку и появился в ePharm; status feed cursor `9` обновлялся. Pending/failed outbox после cutoff — 0. | Физическая выдача на двух кассах и обратный статус заказа не проверены. Заказ с оплатой картой остаётся `pending` и не выдаётся без доверенного подтверждения оплаты — это намеренное fail-closed поведение. |
| Рекомендации и POSM | После backend restart зафиксированы тысячи ответов 200 на `/api/posm/recommend`, без 5xx; новый TaskKiosk и fulfillment выполняются отдельно от рекомендаций. Подписанный обязательный POSM 1.0.64 доставился до всех 409 касс, находившихся онлайн в контрольном срезе. POSM 1.0.65 собран точным main CI, его ZIP и подпись проверены, CDN Range отвечает 206, release зарегистрирован как текущий после backup `app_releases` и негативного теста подменённого манифеста. | Количество онлайн-касс меняется; моментальный переход 1.0.65 нельзя считать доказательством, что каждая офлайн-касса уже обновлена. Кассы без установленного/провижененного POSM автоматически обновиться не могут. |

Rollback merch: исходники и прежний `dist` сохранены локально на 90, а атомарный обмен каталогов обратим. Старые env-файлы root-only сохранены для восстановления конфигурации; возврат прежнего HTTP server key без анализа недопустим. После пересборки `/merch/staff` публичный browser smoke дал HTTP 200 и ноль ошибок консоли. Остаточный операционный риск: корневой диск 90 занят примерно на 98% (~1,3 GiB свободно), поэтому отдельная согласованная уборка/расширение диска обязательна до следующего крупного релиза. Публичный ACC health оставался `ok`, но счётчик ошибок его cache/runtime не равен нулю; транспорт заказа оценивался отдельно по outbox, ACK и status feed.

Срез rollout в 05:07 UTC: 417 из 418 онлайн-касс сообщали `1.0.65.0`; Самал-2 дом 105, Самал-2 дом 45 и Абая 150/230 обновились. Исключение — «Ирбис Транс Систем», Алматы, Сауранбаева 14/2 (`POSM-01KSAHYDSFZRAEDKH18PXNM1A3`): свежий heartbeat всё ещё сообщает `1.0.57.0`. Backend получает от этой кассы успешные запросы проверки версии с целью `1.0.65` примерно каждые 30 секунд, следовательно, проблема после ответа API — на локальном этапе проверки подписи, скачивания или применения. Удалённого доступа к этому Windows-компьютеру и его локального `C:\Epharm\update.log` нет, поэтому причина не подтверждена и кассу нельзя считать обновлённой. Нужен redacted diagnostic archive или удалённый сеанс в согласованное время; рекомендации продолжали работать, а обновление fail-safe. Для 29 активных аптек без provisioned POSM автоматической доставки не будет.

Операционная нагрузка QR после перехода большинства касс с фиксированного 10-секундного опроса на jitter 24–36 секунд снизилась: Nginx 90 регистрировал 2 455 запросов в минуту в 04:50 UTC и 884 в 04:56 UTC; load average за минуту снизился примерно до 1,55. Счётчик backend `active/failure` не вырос выше 1 064 между 04:24 и 05:02 UTC, успешные обращения продолжали расти; `shown/success` вырос до 2. Это наблюдение подтверждает эффект разгрузки, но не заменяет проверку следующего рабочего дня.

Дополнительный обход 90 в 05:10–05:30 UTC выявил два независимых операционных дефекта. **Каталог АСС устарел:** `inkar-medusa-catalog-health.service` сообщает `critical`, источник снимка датирован 2026-09-14 и истёк 2026-09-16. Суточная синхронизация 22 сентября технически завершилась на тех же 28 503 товарах, но не обновила дату данных. Локальный Medusa tunnel `/health` отвечает 200, поэтому это не просто потеря соединения: требуется подтвердить у владельца источника новый актуальный снимок/контракт свежести. Публичный ACC `/api/health` показывает liveness, но не гарантирует актуальность каталога. Не подменять дату снимка и не использовать этот успех health как доказательство актуальных цен/остатков.

**Backup merch CRM восстановлен:** ежедневный `pharmacy-crm-backup.service` не запускался с 19 сентября из-за неисполняемого скрипта; после исправления прав выявилась нехватка `SELECT` у роли приложения для новых таблиц. Отдельный drop-in из PR [#70](https://github.com/sabirovv17/epharm/pull/70) запускает backup под системным `postgres` через peer-аутентификацию, не загружая env приложения с секретами, и сохраняет архивы в отдельном `scheduled` каталоге. `systemd-analyze verify` прошёл; ручной запуск oneshot завершился `Result=success`, timer enabled/active, SHA-256 нового архива проверен. Архив содержит `database.dump`, `storage.tar.gz`, service unit; `pg_restore -l` перечисляет 49 таблиц данных при 49 таблицах в текущей публичной схеме. Старые архивы в родительском каталоге не удалялись. Свободное место всё ещё около 1,2 GiB: ротация в `scheduled` не заменяет расширение/очистку диска по отдельному плану.

**Приёмочный вывод:** transport одного настоящего нового заказа и загрузка QR-портала доказаны; выполнение задания по действующему token на телефоне и полный сценарий cash/demo заказа с двумя кассами не проверены по просьбе владельца отложить выбор аптеки. Эти сценарии остаются обязательным отдельным приёмочным этапом перед демонстрацией бизнес-процесса.
